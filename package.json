{
	"name": "ct-saas",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "lerna exec --scope project-view yarn dev",
		"build": "lerna exec --scope project-view yarn build",
		"docs:dev": "lerna exec --scope project-view yarn docs:dev",
		"docs:build": "lerna exec --scope project-view yarn docs:build"
	},
	"workspaces": [
		"packages/*"
	]
}
